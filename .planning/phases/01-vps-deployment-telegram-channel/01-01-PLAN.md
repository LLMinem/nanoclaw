---
phase: 01-vps-deployment-telegram-channel
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/channels/telegram.ts
  - src/channels/telegram.test.ts
  - src/channels/index.ts
  - src/text-styles.ts
  - src/formatting.test.ts
  - src/router.ts
  - src/index.ts
  - package.json
  - package-lock.json
  - .env.example
autonomous: true
requirements:
  - SETUP-04
  - TELE-01
  - TELE-02

must_haves:
  truths:
    - 'add-telegram skill branch is merged into meeq-claw branch'
    - 'channel-formatting skill branch is merged into meeq-claw branch'
    - 'TypeScript build completes with no errors'
    - 'All tests pass including new Telegram and formatting tests'
    - 'Repository is on the VPS with meeq-claw branch checked out'
  artifacts:
    - path: 'src/channels/telegram.ts'
      provides: 'Telegram channel module with self-registration'
      contains: 'registerChannel'
    - path: 'src/channels/index.ts'
      provides: 'Channel barrel file with telegram import'
      contains: 'telegram'
    - path: 'src/text-styles.ts'
      provides: 'Channel-aware Markdown-to-native formatting'
      contains: 'parseTextStyles'
    - path: 'src/channels/telegram.test.ts'
      provides: 'Telegram channel unit tests'
    - path: 'src/formatting.test.ts'
      provides: 'Channel formatting tests'
  key_links:
    - from: 'src/channels/index.ts'
      to: 'src/channels/telegram.ts'
      via: 'side-effect import'
      pattern: 'import.*telegram'
    - from: 'src/router.ts'
      to: 'src/text-styles.ts'
      via: 'formatOutbound calling parseTextStyles'
      pattern: 'parseTextStyles'
---

<objective>
Merge the add-telegram and channel-formatting skill branches into the meeq-claw working branch, then verify the codebase builds cleanly and all tests pass.

Purpose: Provides the Telegram channel module (grammy-based) and Markdown-to-Telegram formatting — the code foundation that all subsequent plans depend on. Per D-13 and D-14, both skills are installed via git merge of their upstream branches.

Output: Working NanoClaw codebase with Telegram channel support and channel-aware message formatting, ready for infrastructure setup.
</objective>

<execution_context>
@$HOME/.config/opencode/profiles/gsd/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/profiles/gsd/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-vps-deployment-telegram-channel/01-CONTEXT.md
@.claude/skills/add-telegram/SKILL.md
@.claude/skills/channel-formatting/SKILL.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/channels/registry.ts:

```typescript
export interface ChannelOpts {
  onMessage: OnInboundMessage;
  onChatMetadata: OnChatMetadata;
  registeredGroups: () => Record<string, RegisteredGroup>;
}
export type ChannelFactory = (opts: ChannelOpts) => Channel | null;
export function registerChannel(name: string, factory: ChannelFactory): void;
```

From src/channels/index.ts (current state — comments only, no active imports):

```typescript
// Channel self-registration barrel file.
// Each import triggers the channel module's registerChannel() call.
// discord
// gmail
// slack
// telegram
// whatsapp
```

Current git remotes:

```
origin    git@github.com:LLMinem/nanoclaw.git
upstream  git@github.com:qwibitai/nanoclaw.git
```

Current branch: gsd/v1.0-milestone
Target branch for skill merges: meeq-claw (per PROJECT.md constraints)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Merge add-telegram and channel-formatting skill branches</name>
  <files>src/channels/telegram.ts, src/channels/telegram.test.ts, src/channels/index.ts, src/text-styles.ts, src/formatting.test.ts, src/router.ts, src/index.ts, package.json, package-lock.json, .env.example</files>
  <read_first>
    - src/channels/index.ts (current barrel file — confirm no active telegram import exists yet)
    - src/channels/registry.ts (understand registerChannel factory pattern)
    - .claude/skills/add-telegram/SKILL.md (Phase 2 merge instructions)
    - .claude/skills/channel-formatting/SKILL.md (Phase 2 merge instructions)
  </read_first>
  <action>
Per D-13 and D-14, merge both skill branches into the current working branch.

**Important:** The executor is running on the VPS. Per D-09, all commands execute locally on the VPS. Per D-11, explain each step briefly before executing.

**Step 1 — Verify repo state:**

```bash
git status
git branch --show-current
```

Confirm: clean working tree, on `meeq-claw` branch (or `gsd/v1.0-milestone` — if on milestone branch, first check out meeq-claw: `git checkout meeq-claw`).

**Step 2 — Add telegram remote and merge add-telegram skill:**

```bash
git remote add telegram https://github.com/qwibitai/nanoclaw-telegram.git 2>/dev/null || true
git fetch telegram main
git merge telegram/main --no-edit
```

If merge conflicts on `package-lock.json`: resolve with `git checkout --theirs package-lock.json && git add package-lock.json && git merge --continue`.
If merge conflicts on `src/channels/index.ts`: the skill adds `import './telegram.js';` — accept the incoming change while preserving existing comment lines.
For any other conflict: read both sides, reconcile keeping both feature sets.

**Step 3 — Merge channel-formatting skill:**
Upstream remote already exists (`git@github.com:qwibitai/nanoclaw.git`). Fetch the skill branch:

```bash
git fetch upstream skill/channel-formatting
git merge upstream/skill/channel-formatting --no-edit
```

Same conflict resolution strategy: `--theirs` for package-lock.json, manual reconciliation for other files.

This merge adds:

- `src/text-styles.ts` — `parseTextStyles(text, channel)` function
- `src/formatting.test.ts` — 73 tests for formatting across channels
- Modified `src/router.ts` — `formatOutbound` gains optional `channel` parameter
- Modified `src/index.ts` — outbound `sendMessage` paths pass `channel.name` to `formatOutbound`
  </action>
  <verify>
  <automated>git log --oneline -5 && test -f src/channels/telegram.ts && test -f src/text-styles.ts && echo "MERGE_OK"</automated>
  </verify>
  <acceptance_criteria> - `src/channels/telegram.ts` exists and contains `registerChannel('telegram'` - `src/channels/telegram.test.ts` exists - `src/channels/index.ts` contains `import './telegram.js'` (not just a comment) - `src/text-styles.ts` exists and contains `export function parseTextStyles` - `src/formatting.test.ts` exists - `package.json` contains `"grammy"` dependency - `.env.example` contains `TELEGRAM_BOT_TOKEN` - `git status` shows clean working tree (no unresolved conflicts)
  </acceptance_criteria>
  <done>Both skill branches merged cleanly into working branch. Telegram channel module and channel formatting are present in the codebase.</done>
  </task>

<task type="auto">
  <name>Task 2: Install dependencies, build, and run all tests</name>
  <files>package-lock.json, dist/</files>
  <read_first>
    - package.json (verify grammy dependency is present after merge)
    - src/channels/telegram.ts (quick scan — confirm it imports grammy and calls registerChannel)
    - src/text-styles.ts (quick scan — confirm parseTextStyles function exists)
  </read_first>
  <action>
**Step 1 — Install dependencies:**
```bash
npm install
```
This installs the newly-added `grammy` package (Telegram Bot API framework) from the add-telegram merge.

**Step 2 — TypeScript build:**

```bash
npm run build
```

Must complete with zero errors. If build fails:

- Check for import path issues (all imports must use `.js` extension per ESM convention)
- Check for type conflicts between merged branches
- Fix any issues and rebuild

**Step 3 — Run full test suite:**

```bash
npx vitest run
```

All tests must pass, including:

- `src/channels/telegram.test.ts` — Telegram channel unit tests (grammy mock)
- `src/formatting.test.ts` — 73 channel formatting tests
- Any existing tests (`src/channels/registry.test.ts`, `src/db.test.ts`, etc.)

If any test fails, read the failure output, fix the issue, and re-run.
</action>
<verify>
<automated>npm run build 2>&1 | tail -5 && npx vitest run 2>&1 | tail -10</automated>
</verify>
<acceptance_criteria> - `npm run build` exits with code 0 (no TypeScript errors) - `npx vitest run` exits with code 0 (all tests pass) - `dist/` directory contains compiled JavaScript files - `node_modules/grammy/` exists (grammy dependency installed)
</acceptance_criteria>
<done>Codebase builds cleanly and all tests pass. The Telegram channel module and formatting integration are verified at the code level. Ready for infrastructure setup.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary                      | Description                                          |
| ----------------------------- | ---------------------------------------------------- |
| Skill branch → working branch | Untrusted upstream code merged into working codebase |

## STRIDE Threat Register

| Threat ID | Category      | Component          | Disposition | Mitigation Plan                                                                                                                |
| --------- | ------------- | ------------------ | ----------- | ------------------------------------------------------------------------------------------------------------------------------ |
| T-01-01   | T (Tampering) | Skill branch merge | accept      | Skill branches come from the NanoClaw upstream org (qwibitai) — trusted source. Build + test verify code integrity post-merge. |
| T-01-02   | T (Tampering) | package-lock.json  | mitigate    | Run `npm install` (not `npm install --ignore-scripts`) to verify dependency integrity via lockfile checksums.                  |

</threat_model>

<verification>
1. `git log --oneline -10` shows merge commits for both skill branches
2. `npm run build` succeeds with zero errors
3. `npx vitest run` shows all tests passing
4. `grep -c "registerChannel" src/channels/telegram.ts` returns at least 1
5. `grep "import.*telegram" src/channels/index.ts` shows active import (not commented)
6. `grep "parseTextStyles" src/text-styles.ts` confirms formatting function exists
</verification>

<success_criteria>

- add-telegram skill branch merged (TELE-01)
- channel-formatting skill branch merged (TELE-02)
- Repository on VPS with correct branch (SETUP-04)
- Build passes with zero TypeScript errors
- All tests pass (including new Telegram and formatting tests)
- No merge conflicts remain
  </success_criteria>

<output>
After completion, create `.planning/phases/01-vps-deployment-telegram-channel/01-01-SUMMARY.md`
</output>
